You must first install the requirements for PyWeather through pip.

1. In a terminal, navigate to the project directory.
2. run "pip install -r requirements.txt"

This will grab all dependencies for PyWeather!
